# 交易盾H5网页

  不点某公司的内容项目，h5是网页源码，包含主要的交互逻辑。

## 工具与环境

  采用`vuejs`+`ts`技术路线，在付哥提供的模板项目上进行二次开发。  
  安装依赖最好使用`pnpm`，8系列的版本。  
  项目的运行还需要付哥的框架组件`lib-view-mobile`文件夹，该文件夹在付哥的私有仓库里，这个文件夹有些vue组件，整个文件夹放到根目录即可，`无需使用npm`安装其该文件的依赖，即放即用。