# 交易盾Web管理系统

  不点某公司的内容项目，web是后台管理系统，主要用户管理项目相关数据。

## 工具与环境

  采用`vuejs`+`ts`技术路线，在付哥提供的模板项目上进行二次开发。  
  项目的运行还需要付哥的框架组件`nova-lib-view`文件夹，该文件夹在付哥的私有仓库里，这个文件夹有些vue组件，整个文件夹放到根目录即可，`无需使用npm`安装其该文件的依赖，即放即用。  
  这里的`nova-lib-view`有些小改动。若重新从付哥那里获取，web端所有列表的右上角按钮`重置刷新`被点击时，旁边的输入框内容不会清空。需要手动补方法。